// WhatsApp contact link — requires real number from client before launch
// Per brief: not a floating overlay on the tracking result
export function WhatsAppButton() {
  return null; // placeholder until client supplies [NÚMERO]
}
